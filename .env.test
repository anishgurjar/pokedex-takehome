# Loaded only by pytest (after `.env`). Overrides `DATABASE_URL` for test configuration.
# Each test uses a fresh `data/pytest_<uuid>.db` file in the same directory as this path.
DATABASE_URL=sqlite:///./data/poketracker_test.db
