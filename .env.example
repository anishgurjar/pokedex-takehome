# Copy to `.env` and adjust. Used by the app and `uv run alembic` (not by pytest — see `.env.test`).
DATABASE_URL=sqlite:///./data/poketracker.db
JWT_SECRET=dev-secret-change-me-please-use-at-least-32-bytes
